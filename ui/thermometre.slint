
export component Thermometre {

    Rectangle {
        width: 100%;
        height: 100%;
        Image {
            source: @image-url("assets/thermometre.svg");
            width: parent.width;
            height: parent.height;
        }
    }
}
